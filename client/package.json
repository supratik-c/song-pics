{
  "name": "badly-drawn-bangers-web",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:puzzle-index": "node scripts/generatePuzzleIndex.mjs",
    "predev": "npm run generate:puzzle-index",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "npm run generate:puzzle-index",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  }
}